# Xymon task definition for the "la" extension (load average).
# Shipped by the my-xymon-extensions packages into the client's tasks.d
# directory (or append to clientlaunch.cfg).
#
# DISABLED by default: on a host with a full Xymon client the server
# already derives a "cpu" column with the same load information from
# the regular client report - this extension only adds value on hosts
# whose client does not deliver that data. Remove the leading "#" of
# the [la] block to enable it anyway.
#
# (Hosts without a Xymon client - OpenWrt/TurrisOS with the standalone
# runner - ignore this file; xymon-run.sh runs every installed
# extension via cron anyway.)

# The command runs through xymonext.sh, which measures how long
# the extension takes, how much CPU it burns and how many bytes
# it sends, and reports that in the "xymonext" column. Call
# la.sh directly to switch the measurement off for this task
# only; XYMONEXT_ENABLE="no" in xymonext.cfg switches it off
# everywhere.
#
#[la]
#    ENVFILE $XYMONCLIENTHOME/etc/xymonclient.cfg
#    CMD $XYMONCLIENTHOME/ext/xymonext.sh la
#    LOGFILE $XYMONCLIENTLOGS/la.log
#    INTERVAL 5m
