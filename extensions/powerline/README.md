# powerline - server-side PLC monitoring

Server-only collector for HomePlug AV adapters, shipped in
my-xymon-extensions-server and disabled on installation. All
documentation - setup, identity, metrics legend, graphs - is in
[server/README.md](server/README.md), which is the file the package
installs.
