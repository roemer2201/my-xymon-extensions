# Threshold configuration used by the diskio.sh unit tests
# (tests/run.sh). ada0 reports util=42.1 and tank reports riops=200.0
# in the canned data, so both lines below must trigger yellow.

SAMPLE_SECONDS=10

threshold "pd_*" util 40 90
threshold "zp_*" riops 100 -
