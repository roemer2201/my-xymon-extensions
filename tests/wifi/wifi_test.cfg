# Configuration used by the wifi.sh unit tests (tests/run.sh).
# Sourced by wifi.sh via WIFI_CFG.

WIFI_IW="${FAKEIW:?FAKEIW must be exported by the test runner}"
WIFI_UBUS="${FAKEUBUS:?FAKEUBUS must be exported by the test runner}"
WIFI_IWINFO="${FAKEIWINFO:?FAKEIWINFO must be exported by the test runner}"
WIFI_SYSNET="${FAKESYSNET:?FAKESYSNET must be exported by the test runner}"
