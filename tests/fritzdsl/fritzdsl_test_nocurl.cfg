# Configuration used by the fritzdsl.sh unit tests (tests/run.sh):
# curl deliberately points nowhere - the extension must report clear.

CURL="/nonexistent/curl"
FRITZ_USER="monitor"
FRITZ_PASSWORD="secret"
REPORTHOST="fritzbox"
