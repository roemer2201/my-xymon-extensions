# Configuration for the "eMMC present but mmc-utils missing" test run
# (tests/run.sh). Sourced by smart.sh via SMART_CFG.

SMARTCTL="${FAKESMARTCTL:?FAKESMARTCTL must be exported by the test runner}"
USE_SUDO="no"

device /dev/tssd

MMC_UTILS="/nonexistent/mmc"
MMC_DEVICES="/dev/tmmc0"
