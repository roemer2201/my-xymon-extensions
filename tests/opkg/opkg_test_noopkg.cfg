# Configuration used by the opkg.sh unit tests (tests/run.sh):
# no opkg binary on this host -> the extension must report "clear".
# (Pinned to a non-existent path instead of trusting "command -v",
# because the CI host might actually have opkg installed.)

OPKG_BIN="/nonexistent/opkg"
