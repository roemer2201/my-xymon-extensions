# Configuration used by the fritzwan.sh unit tests (tests/run.sh):
# login-free polling via UPnP/IGD only (no credentials).

CURL="${FAKECURL:?FAKECURL must be exported by the test runner}"
FRITZ_HOST="fritz.box"
REPORTHOST="fritzbox"
MODE="igd"
